#dispatch:
# Route the URLs that point to the java backend to backend service
#  - url: "backend-dot-cshsandbox.uw.r.appspot.com/"
#    service: default
# Route all other urls to the React.js frontend
#  - url: "*/api/*"
#    service: backend
    
dispatch:

  - url: "*/"
    service: default
    
  - url: "*/backend"
    service: backend
    
  - url: "*/backend/api/.*"
    service: backend  